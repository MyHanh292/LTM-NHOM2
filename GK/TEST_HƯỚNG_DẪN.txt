"""
🎮 HƯỚNG DẪN TEST CỜ CARO ONLINE
==================================================

CÁCH 1: CHẠY SERVER + 2 CLIENT (EASY)
==================================================
Bước 1: Mở Terminal 1
  $ python server/server.py
  (Sẽ thấy: "Server chạy tại: 0.0.0.0:9999")

Bước 2: Mở Terminal 2 (Client 1)
  $ python offline/login.py
  - Tài khoản: player1 / pass123
  - Chọn "Chơi Online"
  - (Hoặc để mặc định, nó sẽ kết nối tự động)

Bước 3: Mở Terminal 3 (Client 2)
  $ python offline/login.py
  - Tài khoản: player2 / pass123
  - Chọn "Chơi Online"

Khi cả 2 khách kết nối:
  ✓ Server sẽ "Ghép cặp: player1 vs player2"
  ✓ Client 1 sẽ thấy board + tên đối thủ
  ✓ Client 2 sẽ thấy board + tên đối thủ
  ✓ Lượt X (thường player1) sẽ đi trước
  ✓ Bấm ô trên board để đánh
  ✓ Quân sẽ hiển thị ngay trên cả 2 client


CÁCH 2: CHẠY TEST AUTO (BETA)
==================================================
Terminal 1:
  $ python test_2client.py
  
(Sẽ tự động start server + 2 client trong nền)


CÓ VẤN ĐỀ?
==================================================
1. Lỗi "Connection refused"
   → Chắc chắn rằng server đã start bước 1

2. "Chờ đối thủ..." mãi không kết thúc
   → Chắc chắn kết nối 2 client
   → Terminal server sẽ in "Ghép cặp: ..."

3. Click ô không đánh được
   → Kiểm tra status: "✋ TỚI LƯỢT BẠN" chưa?
   → Nếu không, chờ đối thủ đánh xong

4. Không thấy quân của đối thủ
   → Refresh board (F5 trên browser nếu dùng web)
   → Hoặc kiểm tra terminal xem có "MOVE" message không

5. Mất kết nối đột ngột
   → Terminal server có error không?
   → Thử restart server + 2 client


DEBUG MODE
==================================================
Để xem chi tiết message:
  - Terminal server sẽ in: "[HH:MM:SS] message"
  - Terminal client sẽ in: "[username] message"

VD:
  Server: "[14:30:45] player1 (X) đánh: (10, 10)"
  Client: "[player1] Got: MOVE 10 10 X"


TÍNH NĂNG
==================================================
✓ 2 Client chơi online
✓ Board 20x20 
✓ Kiểm tra thắng (5 quân liên tiếp)
✓ Hiển thị tên + avatar đối thủ
✓ Button: Quay lại / Dừng / Thoát
✓ Button: Ván tiếp theo (sau khi kết thúc)


CONFIG
==================================================
Nếu muốn chỉnh server IP (chơi từ 2 máy):
  1. Mở config.json
  2. Đổi "server_ip": "127.0.0.1" 
     Thành: "server_ip": "192.168.1.XXX:9999" (LAN IP server)
  3. Chạy server trên máy 1: python server/server.py
  4. Chạy client trên máy 2: python offline/login.py
     (Client sẽ tự kết nối tới IP trong config.json)

"""
